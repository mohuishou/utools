window.exports = {
  <% features.forEach(feature => { %>
     "<%- feature.code %>": { // 注意：键对应的是plugin.json中的features.code
        mode: "doc", // 文档模式
        args: {
          indexes: require('./<%- feature.code %>/indexes.json'),
          placeholder: "搜索"
        }
      }
  <% }) %>
}